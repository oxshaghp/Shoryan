

export const logIn = ()=>{
    try {
        // Perform login logic here
        // Fetch user data, validate credentials, etc.

    } catch (error) {
        console.error("Login failed:", error);
    } 
}



export const logout = ()=>{
    // Perform logout logic here
    // Clear user session, tokens, etc.
}